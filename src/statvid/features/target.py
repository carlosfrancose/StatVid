"""Compute target variable views_per_day."""
from __future__ import annotations

from typing import Any


def add_views_per_day(df: "Any") -> "Any":
    """Add views_per_day to a dataframe based on views and age."""
    # Intentionally not implemented
    return df

